<!--余额模块-->
<view class="balance">
  <image src="../../images/mywallet.png"></image>
  <view class="txt">
    <text class="money">{{mymoney}}分钟</text>
    <text class="chinese">账户余额</text>
  </view>
</view>
<text class="balance-money">充值金额(1元=10分钟)</text>
<!--充值金额分类模块-->
<view class="balance-demo" id="balanceDemo">
  <form>
      <block wx:for="{{navList}}">
        <button class="type-nav {{curNav == item.id ? 'selected' : ''}}" bindtap="selectNav" data-index="{{index}}" data-id="{{item.id}}" data-money="{{item.money}}">
          <text class="top type-nav {{curNav == item.id ? 'selected' : ''}}" data-index="{{index}}" data-id="{{item.id}}">{{item.chongzhi}}
          </text>
          <text wx:if="{{item.song}}" class="botton type-nav {{curNav == item.id ? 'selected' : ''}}" data-index="{{index}}" data-id="{{item.id}}">{{item.song}}
          </text>
        </button>
		</block>
  </form>
</view>
<!--去充值模块-->
<view class="goblance">
  <view class="txt_t">
    <view>点击去充值，即表示已经阅读并同意 <navigator url="../../pages/service/service">《充值协议》</navigator></view>
    <text>微洗衣不会以任何形式要求您输入银行账号和密码</text>
    <button disabled="{{recharge==0?true:false}}" bindtap="goblance">去充值</button>
    <!--<button bindtap="getRecharge">充值记录</button>-->
  </view>
</view>